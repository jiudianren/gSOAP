编译：






Subscription Manager – Data Preparation (SM-DP)

# auto smsr = SMSRProxyMgr::Instance().GetByOID(Smsr_id.c_str()); 

这个语句的作用

		std::shared_ptr<ServiceBindingProxy> GetByOID(const char *oid)
		{
		
			 static std::unique_ptr<SoapHeaderInfo> QuerySoapHeader(const char *oid, SOAP_ENTITIES entity,
            bool bQueryResponseSoapHeaderInfo = true)
    		{
    			
    			
      		}
		}
